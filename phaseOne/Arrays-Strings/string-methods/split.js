// 🟢 Easy:
// Split a sentence into an array of words.

// Split a string into an array of characters.

// Split a comma-separated string into an array.

// Split a string by the letter "a".

// Split a date string "2025-06-23" by "-".

// 🟡 Intermediate:
// Split a sentence into words and count how many there are.

// Split a CSV string and convert it to numbers.

// Split a string of full names by newline (\n) into an array.

// Split a paragraph into sentences using .split('.').

// Split a string by multiple delimiters (like , and ;) using regex.